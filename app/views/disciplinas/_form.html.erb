<%= form_with(model: disciplina) do |form| %>
  <% if disciplina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disciplina.errors.count, "error") %> prohibited this disciplina from being saved:</h2>

      <ul>
        <% disciplina.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :anoLetivo %>
    <%= form.number_field :anoLetivo %>
  </div>

  <div class="field">
    <%= form.label :turma_id %>
    <%= form.select(:turma_id, options_for_select(@turmas)) %>
  </div>

  <div class="actions">
    <%= form.submit value='submit' %>
  </div>
<% end %>
