<%= form_with(model: avaliacao) do |form| %>
  <% if avaliacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avaliacao.errors.count, "error") %> prohibited this avaliacao from being saved:</h2>

      <ul>
        <% avaliacao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pontos %>
    <%= form.number_field :pontos %>
  </div>

  <div class="field">
    <%= form.label :observacoes %>
    <%= form.text_area :observacoes %>
  </div>

  <div class="field">
    <%= form.label :atividade_id %>
    <%= form.select(:atividade_id, options_for_select(@atividades)) %>
  </div>

  <div class="field">
    <%= form.label :aluno_id %>
    <%= form.select(:aluno_id, options_for_select(@alunos)) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
